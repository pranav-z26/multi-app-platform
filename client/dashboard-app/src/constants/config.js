// Application URLs
export const MAIN_APP_URL = 'http://app.myplatform.local:3000';

// API Endpoints
export const API_ENDPOINTS = {
  DASHBOARD_STATS: '/dashboard/stats',
};

// Error Messages
export const ERROR_MESSAGES = {
  UNAUTHORIZED: 'Unauthorized or session expired. Redirecting to login...',
  FETCH_FAILED: 'Failed to fetch dashboard data. Please try again.',
};

// Timeouts (in milliseconds)
export const REDIRECT_DELAY = 2000;

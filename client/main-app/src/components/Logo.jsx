export default function Logo({ className = '' }) {
  return (
    <div className={`w-22 h-10 bg-blue-600 rounded flex items-center justify-center text-white font-bold text-lg ${className}`}>
      Y-axis
    </div>
  );
}
